<%- include('../partials/header') %>

<section class="error-section container text-center">
  <div class="error-code" aria-hidden="true">403</div>
  <p class="error-label">Không có quyền truy cập</p>
  <h1 class="mb-3">Bạn không được phép truy cập trang này.</h1>
  <p class="text-secondary mx-auto mb-4 error-copy">
    <%= typeof errorMessage === 'string' && errorMessage
      ? errorMessage
      : 'Tài khoản hiện tại không có quyền thực hiện thao tác này trên NTT Marketplace.' %>
  </p>
  <a class="btn btn-primary" href="/">Về trang chủ</a>
</section>

<%- include('../partials/footer') %>
