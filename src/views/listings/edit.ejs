<%- include('../partials/header') %>

<section class="form-page">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-12 col-lg-9 col-xl-8">
        <div class="form-panel">
          <header class="form-panel-header">
            <h1 class="h2">Chỉnh sửa bài đăng</h1>
            <p class="text-secondary">Cập nhật thông tin sản phẩm; người bán và trạng thái không thay đổi tại đây.</p>
          </header>

          <%- include('_form', {
            formAction: '/listings/' + listing._id + '?_method=PUT',
            submitLabel: 'Lưu thay đổi',
            cancelUrl: '/listings/' + listing._id,
            canSubmit: true,
            categories,
            errors,
            oldInput
          }) %>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include('../partials/footer') %>
