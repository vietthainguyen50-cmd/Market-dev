<%- include('../../partials/header', { pageStyles: ['/css/admin.css'] }) %>

<section class="form-page">
  <div class="container admin-shell">
    <%- include('../_navigation', { currentSection: 'categories' }) %>
    <div class="row justify-content-center">
      <div class="col-12 col-lg-8 col-xl-7">
        <div class="form-panel">
          <header class="form-panel-header">
            <h1 class="h2">Thêm danh mục</h1>
            <p class="text-secondary">Tạo nhóm sản phẩm mới cho NTT Marketplace.</p>
          </header>

          <%- include('_form', {
            formAction: '/admin/categories',
            submitLabel: 'Tạo danh mục',
            category,
            errors,
            oldInput
          }) %>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include('../../partials/footer') %>
